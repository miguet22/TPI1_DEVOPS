server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;
    add_header X-Web-Node $hostname always;

    location / {
        try_files $uri $uri/ =404;
    }

}
